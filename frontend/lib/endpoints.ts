/**
 * A collection of all available endpoint URLs
 * {@label ENDPOINT_ENUM}
 */
enum Endpoints {
  USERS = '/users',
  LOGIN = '/login',
  REFRESH = '/token/refresh',
  PROJECTS = '/projects',
  STUDENTS = '/students',
}

export default Endpoints;
